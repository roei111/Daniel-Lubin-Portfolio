import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme) => ({
  dialog: {
    textAlign: "center",
    maxWidth: "400px",
    margin: "0 auto",
  },
  title: {
    paddingBottom: "5px",
  },
  content: {
    color: "#00000087",
  },
  buttons: {
    margin: "0 auto",
  },
}));
